kubectl apply -f sawtooth-kubernetes-default-pbft.yaml
echo "It should take around 10 minutes until all pods are up and
running. use 'kubectl get pods' to check their status or through the web UI using 'minikube dashboard'"
